commands:
  refresh: "fast plugin remove --name iu && plugin add git https://github.com/dev-ideausher/iu_backend_plugin.git"
  starter: "git init && git remote add origin git clone https://github.com/dev-ideausher/nodejs-starter.git && git pull origin master"

